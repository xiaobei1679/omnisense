# ── 运行时态（个人/本地状态，不进仓库）──
/.omni-memory.json
*.omni-memory.json
/.omni-watch.json
*.omni-watch.json
/.omni-traces.json
*.omni-traces.json
/.omni-health-metrics.json
*.omni-health-metrics.json
# 自治自动化运行时日志（agent 本地跑遗留，不入库）
.agent-runs/
# WorkBuddy 本地记忆目录（跨会话记忆，不进仓库）
.workbuddy/

# CodeGraph 本地代码知识图谱索引（大体积二进制，本地/CI 独立重建）
.codegraph/
.env
.env.local
*.local

# ── 依赖与锁文件 ──
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# ── 日志与临时产物 ──
*.log
npm-debug.log*
/tmp/
omni-img-*
# 测试临时产物（agent 写文件验证遗留）
_v.txt
*.tmp
.DS_Store

# ── 系统文件 ──
.DS_Store
Thumbs.db
.idea/
.vscode/
