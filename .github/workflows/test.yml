name: Node.js CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: Syntax check (node --check) across src/test/integrations/scripts/learning
        run: npm run lint
      - name: Run kernel tests (node --test)
        timeout-minutes: 10
        run: npm test
      - name: Run cross-layer integration tests (node --test)
        timeout-minutes: 10
        run: npm run test:link
